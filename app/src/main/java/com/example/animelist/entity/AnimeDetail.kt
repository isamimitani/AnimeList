package com.example.animelist.entity

class AnimeDetail(id: String, gid: String,
                  type: String, name: String,
                  precision: String, vintage: String)
    : AnimeInfo(id, gid, type, name, precision, vintage)